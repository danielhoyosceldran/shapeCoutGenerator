#include <iostream>
#include <fstream>
#include <string.h>
#include <stdio.h>
#include <string.h>

using namespace std;

class coutGenerator
{
private:
    /* data */
public:
    //coutGenerator(/* args */);
    //~coutGenerator();
    void generarTop(ofstream &, char *, int);
    bool generarFitxer(int, int, int);
};